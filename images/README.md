# Best Kept Secret — Image Assets

All live images are loaded from the URLs below.
To host locally: download each file, place it in this `images/` folder,
and update the `src` attributes in `index.html` accordingly.

## Band Logo
- `http://bestkeptsecretband.com/img/logo.png`
  → Used in: nav, hero, footer

## Unsplash Performance Photos
Replace the `?w=` parameter to request different sizes (e.g. `?w=800`).

- `http://bestkeptsecretband.com/img/logo.png`

## Notes
- The YouTube background video ID must be set in `script.js` → `var BKS_ID = 'YOUR_VIDEO_ID'`
- Gallery images can be swapped for real BKS photos from @bestkeptsecretband on Instagram
